# frontend/marketing/.env.local

# Django API base URL — used by blog.ts if any posts need dynamic data
NEXT_PUBLIC_API_URL=http://localhost:8000

# App subdomain — used for login/register CTA links
NEXT_PUBLIC_APP_URL=http://localhost:3001
